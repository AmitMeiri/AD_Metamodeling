# ==============================================================================
# NOTE: The external 'bayesian_metamodeling' library is INTENTIONALLY OMITTED 
# from this file. Because it is a private/local library, adding its local path
# here would break the installation for anyone cloning the repository to a 
# different folder. You MUST install it manually as detailed in the README.
# ==============================================================================

# --- Coupling & Surrogate Only Dependencies ---
# Use this lightweight file if you ONLY want to run the pre-trained metamodel
# and do NOT need to run the heavy base ODE or SuStaIn models from scratch.

# Metamodeling & Shared Data Science Dependencies
numpy>=1.20.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.4.0
seaborn>=0.11.0
pymc
pytensor
pydantic

# Required for the Neural Posterior Estimation (NPE) Surrogate inference
torch>=2.0.0
sbi>=0.21.0
